import styles from './_.module.scss'

export const BookListHeading = () => {
  return (
      <h2 className={styles['booklist-heading']}><span style={{color: '#e3d916'}}>★</span> Mais Populares <span style={{color: '#e3d916'}}>★</span></h2>

  )
}